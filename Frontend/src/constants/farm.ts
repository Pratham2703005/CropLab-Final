export const MASKS_VIEW_MODES = {
  RANGE: 'range',
  MASKS: 'masks',
} as const;

export const DEFAULT_RANGE_OPACITY = 0.7;

export const MAP_MASK_MODES = {
    NDVI: 'ndvi',
    NDWI: 'ndwi',
    NDRE: 'ndre',
    ANOMALY: 'anomaly'
} as const;